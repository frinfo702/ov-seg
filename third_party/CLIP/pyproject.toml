[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.basedpyright]
typeCheckingMode = "standard"
exclude = [
    "**/node_modules",
    "**/__pycache__",
    ".venv",
    ".venv-sam3"
]

[dependency-groups]
dev = [
    "basedpyright>=1.39.4",
]
